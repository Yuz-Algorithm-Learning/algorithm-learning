## 1484 다이어트

### 풀이
```
1. C^2 - T^2 계산을 (C+T)(C-T) 으로 진행
2. 상황에 맞게 3가지 CASE로 나눔  
  * 위의 계산이 G보다 작을 때, curr++
  * G보다 클 때, think++
  * G와 같을 때는 현재 몸무게로 가능한 것이므로 이때의 curr 값을 vector에 삽입
3. sort를 이용해 정렬
```

