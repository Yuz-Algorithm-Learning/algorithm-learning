## 9465 스티커

### 설명
```cpp
- vector<vector<pair<int, int>>> dp;  
    1. first: sticker[i][j] 를 선택하지 않을 때  
    2. second: sticker[i][j] 를 선택할 때  
- 다이나믹 프로그래밍으로 자기자신을 포함했을 때와 포함하지 않았을 때로 나누어 최대 비교하면서 저장
```