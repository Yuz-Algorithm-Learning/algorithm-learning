## 길 찾기 게임

### 풀이
```c++
- 구조체를 이용하여 노드의 y, x 좌표와 인덱스와 해당 노드의 왼쪽, 오른쪽을 담아줄 노드 구조체 저장
- y 좌표 기준 내림차순 -> y 좌표가 같다면 x 좌표 기준 오름차순

/* 노드를 추가해주는 함수 */
void addNode(Node *parent, Node *child);
- 해당 parent의 자식이 없으면 그 위치에다가 저장, 이미 자식이 있다면 그 자식을 부모로 지정해서 재귀
```
